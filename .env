# 数据库连接信息
# 使用 MySQL 数据库（带上海时区 +8）
 DATABASE_URL="mysql://root:root@localhost:3306/auto_tool?timezone=Asia/Shanghai"

# SQLite 连接字符串格式（如果需要使用 SQLite）
#DATABASE_URL="sqlite://db.sqlite3"

# PostgreSQL 连接字符串格式（如果需要使用 PostgreSQL，带上海时区 +8）
# DATABASE_URL="postgres://user:password@localhost:5432/auto_tool?timezone=Asia/Shanghai"
